# 后室：逃出生天 (Backrooms Escape)

扮演 M.E.G.CN 工作人员，从 Level 0 出发，在后室中寻找出口不断切入下一个楼层，直至找到最终出口 Level 399。

## 游戏背景

你是一名 M.E.G.CN（探险者总署中文分部）的探员，在一次任务中不小心切入了后室。这是一个由无数诡异楼层组成的超自然空间。你从著名的 Level 0「前厅」开始，必须穿越一个个危险而诡异的楼层，找到传说中的 Level 399 最终出口，才能回到现实世界。

后室中潜伏着各种危险的实体，但也有前人留下的补给品和线索。能否活着逃出去，取决于你的智慧和运气。

## 快速上手

### 第一步：验证插件

在群里或私聊发送：

```
/br test
```

如果收到回复「✅ 后室逃生插件已正常接收消息！」，说明插件工作正常，可以开始游戏。

### 第二步（可选）：了解背景故事

```
/br teststory
```

通过 NapCat 合并转发消息阅读后室背景故事档案，了解世界观和生存建议。此命令同时可用于测试转发消息功能。

### 第三步：开始游戏

```
/br start
```

你会从 Level 0「前厅」出发，系统会给你当前楼层的描述和可用的命令。

### 第四步：游戏主循环

每一层的标准流程：

```
/br explore  →  探索楼层（搜集物品、了解环境）
/br exit     →  寻找出口（切入下一层）
/br status   →  随时查看探员状态和背包
```

重复以上步骤，直到抵达 Level 399。

### 游戏内智能引导

游戏会在每一轮回复后自动给出下一步建议：

- 进入新楼层后 → 提示先探索再找出口
- 多次找不到出口 → 建议探索搜集物品来提升概率
- 生命值/理智值过低 → 提醒检查背包中的补给品
- 查看背包时 → 根据持有物品给出个性化策略建议

## 命令列表

| 命令 | 说明 | 使用时机 |
|------|------|----------|
| `/br test` | 测试插件连通性 | 首次使用前，验证插件是否正常 |
| `/br teststory` | 查看后室背景故事 | 了解后室世界观（通过合并转发消息展示） |
| `/br start` | 开始新游戏 | 首次进入 / 死亡后重开 |
| `/br explore` | 探索当前楼层 | 每层第一步，搜寻物品和线索 |
| `/br exit` | 尝试寻找出口 | 探索之后，尝试切入下一层 |
| `/br read` | 阅读捡到的纸条 | 探索中捡到纸条后，通过合并转发消息展示内容 |
| `/br use <物品>` | 使用背包中的物品 | 需要恢复生命/理智时使用。例：`/br use o1` |
| `/br status` | 查看探员状态 | 随时查看生命/理智/进度 |
| `/br inventory` | 查看背包 | 检查物品，获取使用建议 |
| `/br help` | 游戏帮助 | 忘记命令时查看 |

## 技术架构

本插件使用 MaiBot SDK 的 `@Command` 组件进行命令路由，当用户消息匹配命令的正则模式时，MaiBot 会自动调度执行对应的处理函数，匹配到的消息不会进入 Planner/LLM 处理链。

 所有游戏命令以 `/br` 为前缀，兼容 NapCat（OneBot v11）消息格式。

### 合并转发消息

`/br teststory` 命令通过 `send.forward()` 发送合并转发消息展示后室档案。

调用方式：
```python
nodes = [
    {
        "user_id": "标识",
        "user_nickname": "昵称",
        "content": [{"type": "text", "data": "消息内容"}],
    },
    ...
]
await self.ctx.send.forward(nodes, stream_id)
```

adapter 内部流程：
1. SDK 将 nodes 包装为 `raw_message = [{"type": "forward", "data": nodes}]`
2. adapter 的 `_contains_forward_segment()` 检测到 `type=forward` 段
3. `_build_forward_nodes()` 映射：`user_id→uin`, `user_nickname→name`, `content` 经 segment encoder 转换
4. → `send_group_forward_msg` / `send_private_forward_msg` → NapCat

> 不用 `send.custom("send_msg", ...)` 传 node 段 — adapter 将其识别为普通消息段输出 `[unsupported:node]`。也不用 `send.custom("send_forward_msg", ...)` — 绕过了 adapter 的消息段转换和 group_id/user_id 自动注入。

### 权限要求

需在 `_manifest.json` 的 `capabilities` 中声明：
```json
"capabilities": [
  "send.text",
  "send.forward",
  ...
]
```

## 游戏机制

### 核心属性

- **生命值（❤️）**：初始 100 点。归零则游戏结束，角色永远留在后室中。
- **理智值（🧠）**：初始 100 点。探索和寻找出口都会消耗理智值。理智值过低会遭受额外惩罚。可通过 o1(杏仁水) 恢复。
- **楼层进度**：从 Level 0 出发，目标是 Level 399。共有 400 个楼层需要穿越。

### 探索（/br explore）

每次探索消耗 **5 点理智值**，会触发以下随机事件：

- **发现线索**：找到 M.E.G. 遗留的信息、地图碎片或日记。
- **获得物品**：有几率找到补给品，如 o1(杏仁水)、o2(急救包)、o3(手电筒) 等。
- **遭遇危险**：可能受伤，或遭遇后室实体。
- **诡异事件**：一些令人不安但不会直接伤害你的事件。

### 寻找出口（/br exit）

每次尝试寻找出口消耗 **10 点理智值**。基础成功率 40%，每次失败后会递增成功率，确保你不会无限卡关。

物品可为成功率提供加成：
- **o4(层级钥匙)**：100% 找到出口
- **o3(手电筒) / o5(M.E.G. 无线电)**：各 +5% 成功率

### 捷径系统

有 12% 的概率触发捷径，可一次跳过 2~20 个楼层，大幅加速进度。

## 知名楼层

游戏包含以下根据后室维基设定的知名楼层，每个都有独特描述和专属实体：

| 楼层 | 名称 | 危险等级 | 特色 |
|------|------|----------|------|
| Level 0 | 前厅 | 低 | 无限黄色办公空间，一切的起点 |
| Level 1 | 宜居区 | 低 | M.E.G. Alpha 基地所在地 |
| Level 2 | 管道梦魇 | 中 | 巨型管道迷宫，猎犬出没 |
| Level 3 | 电气站 | 中 | 遍布电线和配电箱 |
| Level 4 | 废弃办公室 | 中 | 被遗弃的办公大楼 |
| Level 5 | 恐怖旅馆 | 高 | 扭曲奢华的旅馆，镜像实体徘徊 |
| Level 6 | 熄灯 | 高 | 完全黑暗，笑魇聚集地 |
| Level 7 | 深海恐惧 | 极高 | 无边海洋，深海巨兽潜伏 |
| Level 8 | 洞穴系统 | 中 | 巨大的天然洞穴网络 |
| Level 9 | 暗黑郊区 | 中 | 永夜的诡异郊区 |
| Level 10 | 丰收之景 | 中 | 无边麦田，稻草人守望 |
| Level 11 | 无尽城市 | 低 | 空无一人的现代化城市 |
| Level 399 | 真正的结局 | 无 | 最终出口——欢迎回家 |

其他楼层（Level 12~398）将程序化生成，包含不同主题和危险等级。

## 物品系统

探索中有概率获得以下物品：

| 代码 | 名称 | 类型 | 效果 |
|------|------|------|------|
| o1 | 杏仁水 | 消耗品 | 使用后恢复 30 点理智值，在探索和找出口中自动消耗前手动使用 |
| o2 | 急救包 | 消耗品 | 使用后恢复 30 点生命值；在受伤事件中自动消耗一次（减伤 5 点） |
| o3 | 手电筒 | 装备 | 被动生效：遭遇笑魇时驱散（免伤）；其他实体伤害减 10；+5% 出口发现率 |
| o4 | 层级钥匙 | 消耗品 | 持有后下次 `/br exit` 必能找到出口（100% 成功率），使用后消耗 |
| o5 | M.E.G. 无线电 | 装备 | 被动生效：+5% 出口发现率 |
| o6 | 能量棒 | 消耗品 | 使用后恢复 15 点生命值 |
| o7 | 镇定剂 | 消耗品 | 使用后恢复 15 点理智值 |

### 物品获取概率

物品获取分两步：
1. **触发概率**：探索事件中 `give_item` 事件触发时，有 `supply_find_chance`（默认 30%）的概率获得物品
2. **物品权重**：触发后根据各物品的权重随机选择一个物品。权重越高，获得该物品的概率越大

可在 `config.toml` 的 `[game]` 段中调整各物品权重：

## 实体

后室中潜伏着多种危险实体，遇到它们会损失生命值：

- **笑魇**（伤害 15）：黑暗中的无面人形实体，光可以驱散它们
- **猎犬**（伤害 20）：高速四足猛兽
- **窃皮者**（伤害 25）：剥取皮肤的恐怖实体
- **死亡飞蛾**（伤害 10）：能让人产生幻觉的巨型飞蛾
- **旅馆管理者**（伤害 30）：Level 5 的主人
- **深海之物**（伤害 35）：Level 7 的不可名状巨兽
- 以及更多……

## 胜利条件

到达 **Level 399** 并推开最终之门，你就成功逃出了后室！

## 新手策略建议

1. **保持节奏**：每层先 `/br explore` 搜集物品，再用 `/br exit` 找出口。别跳过探索直接找出口，那样会错过补给品。
2. **关注状态**：经常用 `/br status` 和 `/br inventory` 了解自己的情况。生命值或理智值低时优先考虑使用补给。
3. **善用物品**：o4(层级钥匙) 是通关神器，o3(手电筒) 也能提高出口发现率。
4. **不要死磕**：如果同一层 `/br exit` 多次失败，回头 `/br explore` 看看能不能找到 o4(层级钥匙)。
5. **理智管理**：o1(杏仁水) 是关键资源，理智值耗尽会导致额外扣血。保持在 30 以上比较安全。

## 配置说明

可在 `config.toml` 中调整游戏参数和访问控制。配置模型定义在 `config.py` 中，包含五个配置段：`[plugin]`、`[game]`、`[whitelist]`、`[blacklist]`。

### 游戏参数 `[game]`

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `initial_health` | 100 | 初始生命值 |
| `initial_sanity` | 100 | 初始理智值 |
| `explore_sanity_cost` | 5 | 每次探索所消耗的理智值 |
| `exit_search_sanity_cost` | 10 | 每次寻找出口所消耗的理智值 |
| `base_exit_chance` | 0.4 | 成功找到出口的基础概率（0.0~1.0） |
| `exit_chance_increment` | 0.1 | 每次寻找失败后成功概率的提升值 |
| `entity_encounter_chance` | 0.25 | 在楼层中遭遇实体的基础概率 |
| `supply_find_chance` | 0.3 | 探索时发现补给品的概率 |

### 物品权重 `[game]`

每个物品有独立的获取权重（`item_weight_o1` ~ `item_weight_o7`）。当触发物品获取事件时，系统根据权重加权随机选择。权重越高，获得该物品的概率越大。设为 0 则该物品不会出现。

**概率计算方式**：某物品概率 = 该物品权重 ÷ 所有物品权重之和

默认配置下总权重 = 3+3+2+1+2+2+2 = 15：

| 参数 | 默认值 | 对应物品 | 默认概率 |
|------|--------|----------|----------|
| `item_weight_o1` | 3 | o1 杏仁水（恢复 30 理智） | 20.0% |
| `item_weight_o2` | 3 | o2 急救包（恢复 30 生命） | 20.0% |
| `item_weight_o3` | 2 | o3 手电筒（驱散笑魇） | 13.3% |
| `item_weight_o4` | 1 | o4 层级钥匙（稀有，100% 出口） | 6.7% |
| `item_weight_o5` | 2 | o5 M.E.G. 无线电（+5% 出口率） | 13.3% |
| `item_weight_o6` | 2 | o6 能量棒（恢复 15 生命） | 13.3% |
| `item_weight_o7` | 2 | o7 镇定剂（恢复 15 理智） | 13.3% |

> 注意：上述概率仅指获取事件触发后各物品的分配比例。实际获得物品还需要先通过 `supply_find_chance`（默认 30%）的触发判定。

### 白名单 `[whitelist]`

白名单用于控制哪些群组或私聊用户可以使用本插件。检查在命令执行前由 Hook 拦截完成，黑名单优先于白名单。

- **`enabled`**：是否启用白名单（`true`/`false`，默认 `false`）
- **`group_ids`**：群组白名单，字符串数组，填入允许使用的群号
- **`user_ids`**：私聊白名单，字符串数组，填入允许使用的用户 QQ 号
- **`group_deny_message`**：群组不在白名单时的提示文本（可自定义）
- **`private_deny_message`**：用户不在私聊白名单时的提示文本（可自定义）
- **`empty_group_list_message`**：白名单启用但群组列表为空时的提示文本（可自定义）
- **`empty_private_list_message`**：白名单启用但私聊列表为空时的提示文本（可自定义）

**群组和私聊权限完全独立**：你可以只开放群聊而关闭私聊，反之亦然。

```toml
# 示例：仅允许群号 123456789 和 987654321 的群使用
[whitelist]
enabled = true
group_ids = ["123456789", "987654321"]
user_ids = []
```

```toml
# 示例：仅允许 QQ 号 10001 私聊使用，不限群组
[whitelist]
enabled = true
group_ids = []
user_ids = ["10001"]
```

### 黑名单 `[blacklist]`

黑名单用于禁止指定群组或用户使用插件，优先级高于白名单。

- **`enabled`**：是否启用黑名单（`true`/`false`，默认 `false`）
- **`group_ids`**：群组黑名单，字符串数组，填入禁止使用的群号
- **`user_ids`**：用户黑名单，字符串数组，填入禁止使用的用户 QQ 号
- **`group_deny_message`**：群组被黑名单拦截时的提示文本（可自定义）
- **`private_deny_message`**：用户被黑名单拦截时的提示文本（可自定义）

```toml
# 示例：禁止群号 111222333 和用户 444555666 使用
[blacklist]
enabled = true
group_ids = ["111222333"]
user_ids = ["444555666"]
```

## 自定义故事文本

游戏中，玩家在探索时有概率捡到「纸条」。纸条内容来自插件目录下的 `l1_story.txt` ~ `l11_story.txt` 文件（按后室楼层划分），通过合并转发消息展示给玩家。插件会自动发现所有 `l*_story.txt` 文件并加载其中的故事，你可以添加、修改或删除任意文件。

### 文件位置

```
backrooms_escape/
├── l1_story.txt       ← Level 0~1 相关故事（前厅、宜居区）
├── l2_story.txt       ← Level 2 相关故事（管道梦魇）
├── l3_story.txt       ← Level 3 相关故事（电气站）
├── l4_story.txt       ← Level 4 相关故事（废弃办公室）
├── l5_story.txt       ← Level 5 相关故事（恐怖旅馆）
├── l6_story.txt       ← Level 6 相关故事（熄灯）
├── l7_story.txt       ← Level 7 相关故事（深海恐惧）
├── l8_story.txt       ← Level 8 相关故事（洞穴系统）
├── l9_story.txt       ← Level 9 相关故事（暗黑郊区）
├── l10_story.txt      ← Level 10 相关故事（丰收之景）
├── l11_story.txt      ← Level 11 相关故事（无尽城市）
├── plugin.py
├── config.py
├── story.py           ← 故事加载逻辑（自动发现所有 l*_story.txt）
└── ...
```

### 格式说明

每个故事文件内使用 `===STORY_NNN===` 作为分隔符，格式相同：

```
===STORY_001===
第一条故事内容……

===STORY_002===
第二条故事内容……
```

**规则**：

- 每个故事以 `===STORY_` 开头，后跟编号（任意数字或字母），再以 `===` 结尾
- 编号只用于分隔，不影响游戏逻辑（不会显示给玩家）
- 两个分隔符之间的内容即该条故事的正文
- 故事正文支持多行文本，会原样展示给玩家
- 编码使用 **UTF-8**

### 如何新增故事

在任意 `l*_story.txt` 文件末尾追加一个新的故事段：

```
===STORY_009===
你的故事文本写在这里。
可以写多行。
不需要转义，直接写中文即可。
```

也可以新建一个 `l12_story.txt`（或任意匹配 `l*_story.txt` 模式的文件），插件重载后会自动加载。

### 如何修改现有故事

直接编辑对应 `===STORY_NNN===` 下方的内容。分隔符和编号**不要改**，只改正文部分。

### 如何删除故事

删除对应的整个段落（从 `===STORY_NNN===` 到下一个分隔符之前的所有内容）。

### 注意事项

1. **插件重载后生效**：修改任意故事文件后，需要对插件执行卸载/加载（或重启 MaiBot）才能重新读取。
2. **不要删除所有故事**：如果文件中没有有效故事，玩家将不会再捡到纸条。
3. **空行处理**：分隔符之间的空行会被保留，可以利用空行控制段落间距。
4. **编号去重**：建议使用不重复的编号以便管理，但即使编号重复也不影响程序运行。
5. **新文件自动识别**：任何匹配 `l*_story.txt` 模式的新文件都会被自动加载，无需修改代码。

## 数据持久化（存档系统）

插件的玩家游戏进度会自动保存到磁盘，即使插件重载或 MaiBot 重启也不会丢失进度。

### 存档位置

存档文件存放在插件目录下的 `br_data/` 文件夹中：

```
backrooms_escape/
├── br_data/              ← 存档目录（插件首次加载时自动创建）
│   ├── 123456789.json    ← 用户 QQ 号对应的存档文件
│   └── 987654321.json
├── plugin.py
├── config.py
└── ...
```

每个玩家对应一个 JSON 文件，文件名即用户 ID。

### 存档文件结构

```json
{
  "user_id": "123456789",
  "current_level": 5,
  "health": 85,
  "sanity": 72,
  "inventory": [
    {
      "name": "o1",
      "type": "consumable",
      "effect": "sanity_restore",
      "value": 30,
      "display_name": "杏仁水",
      "description": "后室中最常见的补给品…"
    }
  ],
  "game_started": true,
  "exit_attempts": 2,
  "pending_note": null
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `user_id` | string | 玩家 QQ 号 |
| `current_level` | int | 当前所在楼层 |
| `health` | int | 当前生命值 |
| `sanity` | int | 当前理智值 |
| `inventory` | array | 背包物品列表 |
| `game_started` | bool | 游戏是否进行中 |
| `exit_attempts` | int | 当前楼层寻找出口的累计次数 |
| `pending_note` | string\|null | 待阅读的纸条内容，无则为 null |

### 自动保存时机

以下操作完成后会自动保存存档：

| 时机 | 触发操作 |
|------|----------|
| 开始新游戏 | `/br start` |
| 探索完成 | `/br explore`（无论是否遇敌/获得物品） |
| 寻找出口完成 | `/br exit`（无论是否找到出口） |
| 使用物品后 | `/br use <物品>` |
| 查看状态后 | `/br status` |
| 插件卸载 | 插件被重载或 MaiBot 关闭时，批量保存所有在线玩家 |

### 存档清除时机

以下情况会自动删除存档文件：

- 玩家**生命值归零死亡** → 存档被删除，需 `/br start` 重新开始
- 玩家**到达 Level 399 通关** → 存档被删除，可 `/br start` 重新挑战

### 存档恢复

插件每次加载时（包括首次安装和重启），会自动扫描 `br_data/` 目录下的所有 `.json` 文件，将有效存档恢复到内存中。玩家无需手动操作，直接使用 `/br status` 即可继续之前的游戏。

### 注意事项

1. **不要手动编辑存档**：手动修改 JSON 文件可能导致数据格式错误，插件会丢弃损坏的存档并在日志中记录错误。
2. **删除存档**：如需强制清除某玩家的进度，直接删除 `br_data/` 下对应的 `.json` 文件，然后重载插件即可。
3. **跨设备迁移**：将 `br_data/` 文件夹整体复制到另一台机器的同插件目录下，即可迁移所有玩家存档。
